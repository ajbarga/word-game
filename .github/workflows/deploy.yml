name: Deploy
on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write 
    steps:
    - name: Build and Deploy Main Branch
      uses: ajbarga/deploy-gh-pages@main
      with:
        token: ${{ github.token }}
